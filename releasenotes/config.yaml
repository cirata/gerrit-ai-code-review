template: |
  # Remove sections you don't need.
  features:
    -
  issues:
    -
  upgrade:
    -
  deprecations:
    -
  critical:
    -
  security:
    -
  fixes:
    -
  other:
    -
release_tag_re: (?:(^RELEASE_.+(-RPv\d+)$)|(^v.+(-RPv\d+)$)|(?!-SNAPSHOT))
encoding: utf8
